services:
  - type: web
    name: starmock
    runtime: node
    rootDir: app
    buildCommand: npm ci --prefer-offline --no-audit
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/health
