name: 🚀 Deploy to Render

on:
  push:
    branches-ignore:
      - 'dependabot/**'  # skip dependabot auto-branches; all other pushes are evaluated by pre-check gate
  workflow_dispatch:
    inputs:
      skip_tests:
        description: 'Skip tests for emergency deployment'
        required: false
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: render-deploy-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  # ─── Gate: skip entire pipeline for fork + main (main is just a mirror) ───
  pre-check:
    name: 🔎 Pre-check
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.gate.outputs.should_run }}
    steps:
    - name: 🔎 Evaluate deploy eligibility
      id: gate
      run: |
        REPO="${{ github.repository }}"
        BRANCH="${{ github.ref_name }}"
        UPSTREAM="blelian/StarMock"

        if [ "$REPO" != "$UPSTREAM" ] && [ "$BRANCH" = "main" ]; then
          echo "⏭️ Fork + main detected — skipping entire pipeline (main is a mirror, not a deploy target)"
          echo "should_run=false" >> "$GITHUB_OUTPUT"
        else
          echo "✅ Pipeline will run: repo=$REPO branch=$BRANCH"
          echo "should_run=true" >> "$GITHUB_OUTPUT"
        fi

  quality-checks:
    name: 🎨 Code Quality
    needs: pre-check
    if: ${{ needs.pre-check.outputs.should_run == 'true' && github.event.inputs.skip_tests != true }}
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: |
        echo "::group::Installing dependencies"
        npm ci --prefer-offline --no-audit
        echo "::endgroup::"
    
    - name: 🎨 Linting & Formatting
      working-directory: ./app
      run: |
        echo "::group::Running ESLint"
        npm run lint &
        LINT_PID=$!
        echo "::endgroup::"
        
        echo "::group::Checking Prettier formatting"
        npm run format:check &
        FORMAT_PID=$!
        echo "::endgroup::"
        
        # Wait for both to complete
        wait $LINT_PID
        LINT_EXIT=$?
        wait $FORMAT_PID
        FORMAT_EXIT=$?
        
        if [ $LINT_EXIT -ne 0 ]; then
          echo "❌ Linting failed"
          exit 1
        fi
        
        if [ $FORMAT_EXIT -ne 0 ]; then
          echo "❌ Formatting check failed"
          exit 1
        fi
        
        echo "✅ Code quality checks passed!"
  
  tests:
    name: 🧪 Unit Tests
    needs: pre-check
    if: ${{ needs.pre-check.outputs.should_run == 'true' && github.event.inputs.skip_tests != true }}
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: npm ci --prefer-offline --no-audit
    
    - name: 🧪 Run Tests
      working-directory: ./app
      run: |
        echo "::group::Running test suite"
        npm test -- --run --reporter=verbose
        echo "::endgroup::"
  
  integration-tests:
    name: 🔗 Integration Tests
    needs: pre-check
    if: ${{ needs.pre-check.outputs.should_run == 'true' && github.event.inputs.skip_tests != true }}
    runs-on: ubuntu-latest
    
    services:
      mongodb:
        image: mongo:7.0
        ports:
          - 27017:27017
        env:
          MONGO_INITDB_ROOT_USERNAME: testuser
          MONGO_INITDB_ROOT_PASSWORD: testpass
          MONGO_INITDB_DATABASE: starmock_test
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand({ping: 1})'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: npm ci --prefer-offline --no-audit
    
    - name: 🗄️ Wait for MongoDB
      run: |
        echo "Waiting for MongoDB to be ready..."
        for i in {1..30}; do
          if mongosh --host localhost:27017 --username testuser --password testpass --authenticationDatabase admin --eval "db.adminCommand('ping')" > /dev/null 2>&1; then
            echo "✅ MongoDB is ready!"
            break
          fi
          echo "⏳ Waiting for MongoDB... ($i/30)"
          sleep 2
        done
    
    - name: 🚀 Start Backend Server
      working-directory: ./app
      env:
        MONGODB_URI: mongodb://testuser:testpass@localhost:27017/starmock_test?authSource=admin
        SESSION_SECRET: test_secret_key_for_ci_only_not_production
        PORT: 3000
        NODE_ENV: test
      run: |
        echo "Starting backend server..."
        node server.js &
        SERVER_PID=$!
        echo "SERVER_PID=$SERVER_PID" >> $GITHUB_ENV
        
        # Wait for server to start
        for i in {1..30}; do
          if curl -s http://localhost:3000/api/health > /dev/null 2>&1; then
            echo "✅ Server is ready!"
            break
          fi
          echo "⏳ Waiting for server... ($i/30)"
          sleep 2
        done
    
    - name: 🌱 Seed Database
      working-directory: ./app
      env:
        MONGODB_URI: mongodb://testuser:testpass@localhost:27017/starmock_test?authSource=admin
        NODE_ENV: test
      run: |
        echo "::group::Seeding test database"
        npm run seed
        echo "::endgroup::"
    
    - name: 🔗 Run Integration Tests
      working-directory: ./app
      env:
        MONGODB_URI: mongodb://testuser:testpass@localhost:27017/starmock_test?authSource=admin
        SESSION_SECRET: test_secret_key_for_ci_only_not_production
        PORT: 3000
        NODE_ENV: test
      run: |
        echo "::group::Running integration tests"
        node test-integration.js
        echo "::endgroup::"
    
    - name: 🧹 Cleanup
      if: always()
      run: |
        if [ ! -z "$SERVER_PID" ]; then
          echo "Stopping server (PID: $SERVER_PID)..."
          kill $SERVER_PID || true
        fi
  
  e2e-tests:
    name: 🌐 E2E Tests
    needs: pre-check
    if: ${{ needs.pre-check.outputs.should_run == 'true' && github.event.inputs.skip_tests != true }}
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: npm ci --prefer-offline --no-audit
    
    - name: 🎭 Install Playwright Browsers
      working-directory: ./app
      run: npx playwright install --with-deps
    
    - name: 🌐 Run E2E Tests
      working-directory: ./app
      env:
        PLAYWRIGHT_BASE_URL: http://127.0.0.1:4173
        PLAYWRIGHT_WEB_SERVER_COMMAND: npm run dev -- --host 127.0.0.1 --port 4173
      run: |
        echo "::group::Running E2E tests"
        npx playwright test
        echo "::endgroup::"
  
  security:
    name: 🔒 Security Audit
    needs: pre-check
    if: ${{ needs.pre-check.outputs.should_run == 'true' && github.event.inputs.skip_tests != true }}
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: npm ci --prefer-offline --no-audit
    
    - name: 🔒 Security Audit
      working-directory: ./app
      run: |
        echo "::group::Running npm audit"
        npm audit --audit-level=high
        echo "::endgroup::"
  
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    needs: [pre-check, quality-checks, tests, integration-tests, e2e-tests, security]
    if: always() && needs.pre-check.outputs.should_run == 'true' && (needs.quality-checks.result == 'success' || needs.quality-checks.result == 'skipped') && (needs.tests.result == 'success' || needs.tests.result == 'skipped') && (needs.integration-tests.result == 'success' || needs.integration-tests.result == 'skipped') && (needs.e2e-tests.result == 'success' || needs.e2e-tests.result == 'skipped') && (needs.security.result == 'success' || needs.security.result == 'skipped')
    outputs:
      build_time: ${{ steps.timing.outputs.build_time }}
      
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🔧 Configure Git line endings
      run: |
        git config --global core.autocrlf false
        git config --global core.eol lf
    
    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'npm'
        cache-dependency-path: app/package-lock.json
    
    - name: 📦 Install dependencies
      working-directory: ./app
      run: npm ci --prefer-offline --no-audit
    
    - name: 🏗️ Build Application
      id: timing
      working-directory: ./app
      run: |
        START_TIME=$(date +%s)
        echo "::group::Building production bundle"
        npm run build
        echo "::endgroup::"
        END_TIME=$(date +%s)
        BUILD_TIME=$((END_TIME - START_TIME))
        echo "build_time=${BUILD_TIME}s" >> $GITHUB_OUTPUT
        
        echo "✅ Build completed in ${BUILD_TIME}s"
        echo "📊 Build artifacts:"
        ls -lh dist/
  
  deploy:
    name: 🚀 Deploy to Render
    needs: build
    runs-on: ubuntu-latest
    outputs:
      deploy_time: ${{ steps.deploy_timing.outputs.deploy_time }}
      deploy_status: ${{ steps.deploy_timing.outputs.deploy_status }}
      deploy_target: ${{ steps.deploy_timing.outputs.deploy_target }}
      app_url: ${{ steps.set_env.outputs.app_url }}
      skip_deploy: ${{ steps.set_env.outputs.skip_deploy }}
      skip_reason: ${{ steps.set_env.outputs.skip_reason }}
    
    steps:
    - name: 🎯 Set Deployment Target
      id: set_env
      run: |
        echo "🔍 DEBUG - Repo: ${{ github.repository }}"
        echo "🔍 DEBUG - Branch: ${{ github.ref_name }}"

        # Determine which secrets to use based on repository and branch
        # Rules (works for ALL contributors):
        #   - Upstream (blelian/StarMock) + main  → Production deploy
        #   - Any fork + working branch → Dev deploy (each contributor's own Render)
        #   - Any fork + main → Skip (main is just a mirror, not a deploy target)
        #   - Upstream + non-main branch → Dev deploy if secrets exist, else skip
        UPSTREAM="blelian/StarMock"

        if [ "${{ github.repository }}" != "$UPSTREAM" ] && [ "${{ github.ref_name }}" != "main" ]; then
          # Fork working branch → deploy to contributor's dev Render
          DEPLOY_HOOK_URL=$(echo "${{ secrets.RENDER_DEPLOY_HOOK_URL_DEV }}" | xargs)
          APP_URL=$(echo "${{ secrets.RENDER_APP_URL_DEV }}" | xargs)

          echo "🔍 DEBUG - Fork (${{ github.repository }}) working branch (${{ github.ref_name }}) - using dev secrets"
          echo "🔍 DEBUG - Dev deploy hook length: ${#DEPLOY_HOOK_URL}"
          echo "🔍 DEBUG - Dev app URL length: ${#APP_URL}"

          echo "deploy_target=Development (${{ github.ref_name }})" >> "$GITHUB_OUTPUT"

          echo "📍 Deploying to: Development (${{ github.ref_name }})"
          echo "🌐 Target URL: $APP_URL"
        elif [ "${{ github.repository }}" != "$UPSTREAM" ] && [ "${{ github.ref_name }}" = "main" ]; then
          # Fork main branch → skip deploy (main is a mirror, not a deploy target)
          echo "🔍 DEBUG - Fork (${{ github.repository }}) main branch - skipping deployment (main is a mirror)"
          echo "deploy_target=Skipped (fork main)" >> "$GITHUB_OUTPUT"
          echo "skip_deploy=true" >> "$GITHUB_OUTPUT"
          echo "skip_reason=Fork main branch does not deploy. Only working branches deploy to dev Render." >> "$GITHUB_OUTPUT"
          echo "📍 Skipping deployment: Fork main is a mirror, not a deploy target"
        elif [ "${{ github.repository }}" = "$UPSTREAM" ] && [ "${{ github.ref_name }}" = "main" ]; then
          # Use prod secrets for main branch in upstream repository
          DEPLOY_HOOK_URL=$(echo "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" | xargs)
          APP_URL=$(echo "${{ secrets.RENDER_APP_URL }}" | xargs)

          echo "🔍 DEBUG - Upstream main branch - using prod secrets"
          echo "🔍 DEBUG - Production deploy hook length: ${#DEPLOY_HOOK_URL}"
          echo "🔍 DEBUG - Production app URL length: ${#APP_URL}"
          echo "🔍 DEBUG - Deploy hook value: [$DEPLOY_HOOK_URL]"
          echo "🔍 DEBUG - App URL value: [$APP_URL]"

          echo "deploy_target=Production" >> "$GITHUB_OUTPUT"

          echo "📍 Deploying to: Production (Main Branch)"
          echo "🌐 Target URL: $APP_URL"
        elif [ "${{ github.repository }}" = "$UPSTREAM" ]; then
          # Upstream non-main branch — check if dev secrets are configured
          DEV_DEPLOY_HOOK_URL=$(echo "${{ secrets.RENDER_DEPLOY_HOOK_URL_DEV }}" | xargs)
          DEV_APP_URL=$(echo "${{ secrets.RENDER_APP_URL_DEV }}" | xargs)
          
          if [ -n "$DEV_APP_URL" ] && [ -n "$DEV_DEPLOY_HOOK_URL" ]; then
            # Use dev secrets if configured in upstream
            DEPLOY_HOOK_URL="$DEV_DEPLOY_HOOK_URL"
            APP_URL="$DEV_APP_URL"
            
            echo "🔍 DEBUG - Upstream branch with dev secrets - using dev secrets"
            echo "🔍 DEBUG - Dev deploy hook length: ${#DEPLOY_HOOK_URL}"
            echo "🔍 DEBUG - Dev app URL length: ${#APP_URL}"
            
            echo "deploy_target=Development (${{ github.ref_name }})" >> "$GITHUB_OUTPUT"
            
            echo "📍 Deploying to: Development (${{ github.ref_name }})"
            echo "🌐 Target URL: $APP_URL"
          else
            # Skip deployment if no dev secrets configured in upstream
            echo "🔍 DEBUG - Upstream branch without dev secrets - skipping deployment"
            echo "deploy_target=Skipped (${{ github.ref_name }})" >> "$GITHUB_OUTPUT"
            echo "skip_deploy=true" >> "$GITHUB_OUTPUT"
            echo "skip_reason=Branch deployments require RENDER_APP_URL_DEV and RENDER_DEPLOY_HOOK_URL_DEV secrets to be configured in the upstream repository." >> "$GITHUB_OUTPUT"
            
            echo "📍 Skipping deployment: Dev secrets not configured in upstream repository"
          fi
        fi
        echo "deploy_hook=$DEPLOY_HOOK_URL" >> "$GITHUB_OUTPUT"
        echo "app_url=$APP_URL" >> "$GITHUB_OUTPUT"
        
        echo "🔍 DEBUG - GITHUB_OUTPUT file contents:"
        cat "$GITHUB_OUTPUT" | tail -20
        # If required secrets are missing, mark this run to skip deploy.
        if [ -z "$APP_URL" ] || [ -z "$DEPLOY_HOOK_URL" ]; then
          echo "⚠️ WARNING: Deployment secrets missing or empty. Skipping deploy step."
          echo "skip_deploy=true" >> "$GITHUB_OUTPUT"

          # Provide a readable reason for logs and dashboard
          if [ -z "$APP_URL" ] && [ -z "$DEPLOY_HOOK_URL" ]; then
            SKIP_REASON="Missing both RENDER_APP_URL and RENDER_DEPLOY_HOOK_URL (or their DEV equivalents). This often happens for PRs from forks where repository secrets are not available to the runner."
          elif [ -z "$APP_URL" ]; then
            SKIP_REASON="Missing RENDER_APP_URL (or RENDER_APP_URL_DEV)."
          else
            SKIP_REASON="Missing RENDER_DEPLOY_HOOK_URL (or RENDER_DEPLOY_HOOK_URL_DEV)."
          fi

          echo "skip_reason=$SKIP_REASON" >> "$GITHUB_OUTPUT"
          echo "🔍 DEBUG - Skip reason: $SKIP_REASON"
        else
          echo "skip_deploy=false" >> "$GITHUB_OUTPUT"
        fi
    
    - name: � Verify Step Outputs
      run: |
        echo "🔍 Verifying step outputs are accessible..."
        echo "🔍 Repo: ${{ github.repository }}"
        echo "🔍 Branch: ${{ github.ref_name }}"
        echo "Deploy Hook: [${{ steps.set_env.outputs.deploy_hook }}]"
        echo "App URL: [${{ steps.set_env.outputs.app_url }}]"
        echo "Deploy Target: [${{ steps.set_env.outputs.deploy_target }}]"
        SKIP_DEPLOY="${{ steps.set_env.outputs.skip_deploy }}"
        SKIP_REASON="${{ steps.set_env.outputs.skip_reason }}"

        if [ "$SKIP_DEPLOY" = "true" ]; then
          echo "⚠️ Skipping deploy: $SKIP_REASON"
          echo "✅ Verify step completed — deploy will be skipped for this run."
          exit 0
        fi

        if [ -z "${{ steps.set_env.outputs.app_url }}" ]; then
          echo "❌ ERROR: app_url output is empty in same job and skip_deploy is not set!"
          echo "🔍 This could indicate the secret is missing (not set in this repository) or contains only whitespace/newlines."
          echo "🔍 If this run is from a fork or a contributor repo, check that RENDER_APP_URL_DEV is set in the repository secrets."
          exit 1
        fi

        echo "✅ Step outputs verified successfully"
    
    - name: �🚀 Trigger Render Deploy Hook
      id: deploy_timing
      run: |
        START_TIME=$(date +%s)
        DEPLOY_HOOK="${{ steps.set_env.outputs.deploy_hook }}"
        DEPLOY_TARGET="${{ steps.set_env.outputs.deploy_target }}"

          SKIP_DEPLOY="${{ steps.set_env.outputs.skip_deploy }}"
          SKIP_REASON="${{ steps.set_env.outputs.skip_reason }}"

          if [ "$SKIP_DEPLOY" = "true" ]; then
            echo "🚫 Skipping Render deploy: $SKIP_REASON"
            END_TIME=$(date +%s)
            DEPLOY_TIME=$((END_TIME - START_TIME))
            echo "deploy_time=${DEPLOY_TIME}s" >> $GITHUB_OUTPUT
            echo "deploy_status=skipped" >> $GITHUB_OUTPUT
            echo "deploy_target=$DEPLOY_TARGET" >> $GITHUB_OUTPUT
            echo "✅ Marked deploy as skipped."
            exit 0
          fi
        
        echo "🔍 DEBUG - Final deploy hook: [$DEPLOY_HOOK]"
        echo "🔍 DEBUG - Deploy hook length: ${#DEPLOY_HOOK}"
        
        # Validate deploy hook is not empty
        if [ -z "$DEPLOY_HOOK" ]; then
          echo "❌ ERROR: Deploy hook URL is empty!"
          echo "🔍 Check that the secret RENDER_DEPLOY_HOOK_URL (production) or RENDER_DEPLOY_HOOK_URL_DEV is configured"
          echo "deploy_time=0s" >> $GITHUB_OUTPUT
          echo "deploy_status=failed" >> $GITHUB_OUTPUT
          echo "deploy_target=$DEPLOY_TARGET" >> $GITHUB_OUTPUT
          exit 1
        fi
        
        # Validate deploy hook starts with https://
        if [[ ! "$DEPLOY_HOOK" =~ ^https:// ]]; then
          echo "❌ ERROR: Deploy hook URL is malformed: [$DEPLOY_HOOK]"
          echo "🔍 URL should start with 'https://'"
          exit 1
        fi
        
        echo "🚀 Triggering deployment to: $DEPLOY_TARGET"
        echo "🔗 Branch: ${{ github.ref_name }}"
        
        RESPONSE=$(curl -X POST "$DEPLOY_HOOK" \
          -H "Content-Type: application/json" \
          -w "\n%{http_code}" \
          -s \
          --max-time 30)
        
        HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
        BODY=$(echo "$RESPONSE" | head -n-1)
        END_TIME=$(date +%s)
        DEPLOY_TIME=$((END_TIME - START_TIME))
        
        echo "deploy_time=${DEPLOY_TIME}s" >> $GITHUB_OUTPUT
        echo "deploy_target=$DEPLOY_TARGET" >> $GITHUB_OUTPUT
        echo "HTTP Status: $HTTP_CODE"
        echo "Response: $BODY"
        echo "⚡ Deploy hook responded in ${DEPLOY_TIME}s"
        
        if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 201 ]; then
          echo "deploy_status=success" >> $GITHUB_OUTPUT
          echo "✅ Deployment triggered successfully to $DEPLOY_TARGET!"
          echo "🔄 Render is now building and deploying your application..."
        else
          echo "deploy_status=failed" >> $GITHUB_OUTPUT
          echo "❌ Deployment hook failed with status $HTTP_CODE"
          exit 1
        fi
  
  healthcheck:
    name: 🏥 Health Check
    needs: [build, deploy]
    if: ${{ needs.deploy.outputs.deploy_status == 'success' || needs.deploy.outputs.deploy_status == 'skipped' }}
    runs-on: ubuntu-latest
    outputs:
      health_status: ${{ steps.health.outputs.health_status }}
      health_time: ${{ steps.health.outputs.health_time }}
      health_attempts: ${{ steps.health.outputs.health_attempts }}
    
    steps:
    - name: ⏳ Wait for Render Deployment
      run: |
        echo "⏳ Giving Render time to build and deploy..."
        echo "📊 Estimated time: 2-3 minutes"
        
        # Smart wait with progress indicators
        for i in {1..6}; do
          echo "⏰ Waiting... ($i/6) - $((i * 15))s elapsed"
          sleep 15
        done
        
        echo "✅ Initial wait complete. Starting health checks..."
    
    - name: 🔍 Health Check with Smart Retry
      id: health
      run: |
        # If deploy job marked this run as skipped, skip healthcheck gracefully.
        if [ "${{ needs.deploy.outputs.deploy_status }}" = "skipped" ]; then
          echo "⏭️ Deployment was skipped — skipping healthcheck."
          echo "🔍 Reason: ${{ needs.deploy.outputs.skip_reason }}"
          echo "health_status=skipped" >> $GITHUB_OUTPUT
          echo "health_time=0s" >> $GITHUB_OUTPUT
          echo "health_attempts=0" >> $GITHUB_OUTPUT
          exit 0
        fi
        
        # Use the app_url output from the deploy job (single source of truth)
        APP_URL="${{ needs.deploy.outputs.app_url }}"
        
        MAX_RETRIES=12
        RETRY_COUNT=0
        HEALTH_URL="${APP_URL}/api/health"
        START_TIME=$(date +%s)
        SUCCESS=false
        WAIT_TIME=10
        
        echo "🔍 DEBUG - App URL: [$APP_URL]"
        echo "🔍 DEBUG - Health URL: [$HEALTH_URL]"
        
        # Validate health URL is not empty
        if [ -z "$APP_URL" ]; then
          echo "❌ ERROR: App URL is empty!"
          echo "🔍 Check that RENDER_APP_URL or RENDER_APP_URL_DEV secret is configured"
          echo "health_status=failed" >> $GITHUB_OUTPUT
          echo "health_time=0s" >> $GITHUB_OUTPUT
          echo "health_attempts=0" >> $GITHUB_OUTPUT
          exit 1
        fi
        
        DEPLOY_TARGET="${{ needs.deploy.outputs.deploy_target }}"
        echo "🏥 Checking health endpoint: $HEALTH_URL"
        echo "📍 Target: $DEPLOY_TARGET"
        echo "🔄 Max retries: $MAX_RETRIES with exponential backoff"
        echo ""
        
        while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
          RETRY_COUNT=$((RETRY_COUNT + 1))
          
          # Make request with timeout
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" \
            --max-time 10 \
            --connect-timeout 5 \
            "$HEALTH_URL" 2>/dev/null || echo "000")
          
          CURRENT_TIME=$(date +%s)
          ELAPSED=$((CURRENT_TIME - START_TIME))
          
          if [ "$RESPONSE" -eq 200 ]; then
            echo "✅ Health check PASSED! (Attempt $RETRY_COUNT/$MAX_RETRIES)"
            echo "⚡ Application is live and responding"
            echo "⏱️ Total time: ${ELAPSED}s"
            echo "health_time=${ELAPSED}s" >> $GITHUB_OUTPUT
            echo "health_attempts=$RETRY_COUNT" >> $GITHUB_OUTPUT
            echo "health_status=success" >> $GITHUB_OUTPUT
            SUCCESS=true
            break
          elif [ "$RESPONSE" -eq 000 ]; then
            # Connection failed - network error
            echo "❌ Attempt $RETRY_COUNT/$MAX_RETRIES: Connection failed - network error (${ELAPSED}s elapsed)"
          elif [ "$RESPONSE" -ge 400 ] && [ "$RESPONSE" -lt 600 ]; then
            # HTTP error response - fail immediately
            echo "❌ Health check FAILED! HTTP $RESPONSE error (Attempt $RETRY_COUNT/$MAX_RETRIES)"
            echo "🔍 Application returned error status - deployment may have issues"
            echo "health_time=${ELAPSED}s" >> $GITHUB_OUTPUT
            echo "health_attempts=$RETRY_COUNT" >> $GITHUB_OUTPUT
            echo "health_status=failed" >> $GITHUB_OUTPUT
            exit 1
          else
            # Other response codes (3xx, etc.) - retry
            echo "⏳ Attempt $RETRY_COUNT/$MAX_RETRIES: Status $RESPONSE (${ELAPSED}s elapsed)"
          fi
          
          # Exponential backoff: 10s, 15s, 22s, 33s, ... capped at 60s
          echo "⏳ Waiting ${WAIT_TIME}s before next retry..."
          sleep $WAIT_TIME
          WAIT_TIME=$(( WAIT_TIME * 3 / 2 ))
          if [ $WAIT_TIME -gt 60 ]; then
            WAIT_TIME=60
          fi
        done
        
        if [ "$SUCCESS" = false ]; then
          # Health check failed
          FINAL_TIME=$(date +%s)
          TOTAL_TIME=$((FINAL_TIME - START_TIME))
          
          echo "⚠️ Health check timeout after ${TOTAL_TIME}s"
          echo "health_time=${TOTAL_TIME}s" >> $GITHUB_OUTPUT
          echo "health_attempts=$MAX_RETRIES" >> $GITHUB_OUTPUT
          echo "health_status=timeout" >> $GITHUB_OUTPUT
        fi
  
  dashboard:
    name: 📊 Deployment Dashboard
    needs: [pre-check, quality-checks, tests, security, build, deploy, healthcheck]
    runs-on: ubuntu-latest
    if: always() && needs.pre-check.outputs.should_run == 'true'
    
    steps:
    - name: 📊 Generate Enhanced Deployment Dashboard
      run: |
        # Determine overall status
        QUALITY_STATUS="${{ needs.quality-checks.result }}"
        TESTS_STATUS="${{ needs.tests.result }}"
        SECURITY_STATUS="${{ needs.security.result }}"
        BUILD_STATUS="${{ needs.build.result }}"
        DEPLOY_STATUS="${{ needs.deploy.result }}"
        HEALTH_STATUS="${{ needs.healthcheck.outputs.health_status }}"
        
        # Status emojis and badges
        get_status_emoji() {
          case "$1" in
            "success") echo "✅" ;;
            "skipped") echo "⏭️" ;;
            "failed") echo "❌" ;;
            "failure") echo "❌" ;;
            "cancelled") echo "🚫" ;;
            "timeout") echo "⏱️" ;;
            *) echo "❓" ;;
          esac
        }
        
        get_status_badge() {
          case "$1" in
            "success") echo "🟢 PASSED" ;;
            "skipped") echo "⚪ SKIPPED" ;;
            "failed") echo "🔴 FAILED" ;;
            "failure") echo "🔴 FAILED" ;;
            "cancelled") echo "⚫ CANCELLED" ;;
            "timeout") echo "🟡 TIMEOUT" ;;
            *) echo "⚪ UNKNOWN" ;;
          esac
        }
        
        # Overall deployment status with color indicators
        if [ "$DEPLOY_STATUS" = "success" ] && [ "$HEALTH_STATUS" = "success" ]; then
          OVERALL_STATUS="SUCCESS"
          OVERALL_EMOJI="🎉"
          OVERALL_COLOR="🟢"
        elif [ "$DEPLOY_STATUS" = "success" ] && [ "$HEALTH_STATUS" = "skipped" ]; then
          OVERALL_STATUS="DEPLOYED (Health Check Skipped)"
          OVERALL_EMOJI="⏭️"
          OVERALL_COLOR="🟡"
        elif [ "$HEALTH_STATUS" = "skipped" ]; then
          OVERALL_STATUS="SKIPPED (No deployment)"
          OVERALL_EMOJI="⏭️"
          OVERALL_COLOR="⚪"
        elif [ "$DEPLOY_STATUS" = "success" ] && [ "$HEALTH_STATUS" = "failed" ]; then
          OVERALL_STATUS="DEPLOYED (Health Check Failed)"
          OVERALL_EMOJI="⚠️"
          OVERALL_COLOR="🔴"
        elif [ "$DEPLOY_STATUS" = "success" ] && [ "$HEALTH_STATUS" = "timeout" ]; then
          OVERALL_STATUS="DEPLOYED (Health Check Timeout)"
          OVERALL_EMOJI="⚠️"
          OVERALL_COLOR="🟡"
        else
          OVERALL_STATUS="FAILED"
          OVERALL_EMOJI="💥"
          OVERALL_COLOR="🔴"
        fi
        
        # Create enhanced dashboard with better visual flow
        cat << 'EOF' >> $GITHUB_STEP_SUMMARY
        <div align="center">
        
        # 🚀 StarMock Deployment Dashboard

        EOF

        echo "## $OVERALL_COLOR $OVERALL_EMOJI **$OVERALL_STATUS** $OVERALL_EMOJI $OVERALL_COLOR" >> $GITHUB_STEP_SUMMARY

        cat << 'EOF' >> $GITHUB_STEP_SUMMARY
        </div>
        
        ---
        
        ### 📋 Deployment Information
        
        | | |
        |---|---|
        | **🌿 Branch** | `${{ github.ref_name }}` |
        | **📍 Target** | ${{ needs.deploy.outputs.deploy_target }} |
        | **📦 Commit** | [`${{ github.sha }}`](https://github.com/${{ github.repository }}/commit/${{ github.sha }}) |
        | **👤 Triggered by** | @${{ github.actor }} |
        | **🕐 Time** | ${{ github.event.head_commit.timestamp }} |
        | **🔗 Workflow Run** | [#${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) |
        
        ---
        
        ## � Pipeline Execution Flow
        
        ```mermaid
        %%{init: {'theme':'base', 'themeVariables': { 'primaryColor':'#e1f5ff','primaryTextColor':'#000','primaryBorderColor':'#0366d6','lineColor':'#6a737d','secondaryColor':'#fff3e0','tertiaryColor':'#f3e5f5'}}}%%
        graph TB
            Start([🚀 Deploy Triggered]) --> Parallel{⚡ Parallel Stage}
            
            Parallel --> QC[🎨 Code Quality<br/>ESLint + Prettier]
            Parallel --> UT[🧪 Unit Tests<br/>Vitest Suite]
            Parallel --> SA[🔒 Security Audit<br/>npm audit]
            
            QC --> Build[🏗️ Build<br/>Production Bundle]
            UT --> Build
            SA --> Build
            
            Build --> Deploy[🚀 Deploy<br/>Render Hook]
            Deploy --> Health[🏥 Health Check<br/>Smart Retry]
            Health --> Dashboard[📊 Dashboard<br/>This Report]
            
            Dashboard --> End([✅ Complete])
            
            style Start fill:#e8f5e9,stroke:#4caf50,stroke-width:3px
            style Parallel fill:#fff3e0,stroke:#ff9800,stroke-width:2px
            style QC fill:#e1f5ff,stroke:#2196f3
            style UT fill:#e1f5ff,stroke:#2196f3
            style SA fill:#e1f5ff,stroke:#2196f3
            style Build fill:#fff3e0,stroke:#ff9800
            style Deploy fill:#f3e5f5,stroke:#9c27b0
            style Health fill:#e8f5e9,stroke:#4caf50
            style Dashboard fill:#fce4ec,stroke:#e91e63
            style End fill:#e8f5e9,stroke:#4caf50,stroke-width:3px
        ```
        
        ---
        
        ## � Pipeline Stages Status
        
        <table>
        <tr>
        <th>Stage</th>
        <th>Status</th>
        <th>Details</th>
        <th>Duration</th>
        </tr>
        <tr>
        <td>�🎨 <strong>Code Quality</strong></td>
        <td>$(get_status_badge "$QUALITY_STATUS")</td>
        <td>ESLint + Prettier validation</td>
        <td><em>Parallel</em></td>
        </tr>
        <tr>
        <td>🧪 <strong>Unit Tests</strong></td>
        <td>$(get_status_badge "$TESTS_STATUS")</td>
        <td>Vitest test suite execution</td>
        <td><em>Parallel</em></td>
        </tr>
        <tr>
        <td>🔒 <strong>Security Audit</strong></td>
        <td>$(get_status_badge "$SECURITY_STATUS")</td>
        <td>npm audit (high severity)</td>
        <td><em>Parallel</em></td>
        </tr>
        <tr>
        <td>🏗️ <strong>Build</strong></td>
        <td>$(get_status_badge "$BUILD_STATUS")</td>
        <td>Production bundle compilation</td>
        <td><code>${{ needs.build.outputs.build_time }}</code></td>
        </tr>
        <tr>
        <td>🚀 <strong>Deploy</strong></td>
        <td>$(get_status_badge "$DEPLOY_STATUS")</td>
        <td>Render deployment hook</td>
        <td><code>${{ needs.deploy.outputs.deploy_time }}</code></td>
        </tr>
        <tr>
        <td>🏥 <strong>Health Check</strong></td>
        <td>$(get_status_badge "$HEALTH_STATUS")</td>
        <td>Endpoint verification (${{ needs.healthcheck.outputs.health_attempts }} attempts)</td>
        <td><code>${{ needs.healthcheck.outputs.health_time }}</code></td>
        </tr>
        </table>
        
        ---
        
        ## ⚡ Performance Metrics
        
        <table>
        <tr>
        <td align="center">
        <h3>🏗️</h3>
        <strong>Build Time</strong><br/>
        <code>${{ needs.build.outputs.build_time }}</code>
        </td>
        <td align="center">
        <h3>🚀</h3>
        <strong>Deploy Hook</strong><br/>
        <code>${{ needs.deploy.outputs.deploy_time }}</code>
        </td>
        <td align="center">
        <h3>🏥</h3>
        <strong>Health Check</strong><br/>
        <code>${{ needs.healthcheck.outputs.health_time }}</code>
        </td>
        <td align="center">
        <h3>⚡</h3>
        <strong>Parallel Jobs</strong><br/>
        <code>3x faster</code>
        </td>
        </tr>
        </table>
        
        ---
        
        EOF
        
        # Get deployment URL based on branch (same as deploy/healthcheck jobs)
        if [ "${{ github.ref_name }}" = "main" ]; then
          DEPLOY_URL=$(echo "${{ secrets.RENDER_APP_URL }}" | xargs)
        else
          DEPLOY_URL=$(echo "${{ secrets.RENDER_APP_URL_DEV }}" | xargs)
        fi
        
        # Deployment-specific sections
        if [ "$DEPLOY_STATUS" = "success" ]; then
          DEPLOY_TARGET="${{ needs.deploy.outputs.deploy_target }}"
          cat << EOF >> $GITHUB_STEP_SUMMARY
        ## 🌐 Live Deployment
        
        <div align="center">
        
        ### 📍 **$DEPLOY_TARGET**
        
        🎯 **Application URL:** [$DEPLOY_URL]($DEPLOY_URL)
        
        </div>
        
        | Resource | Link |
        |----------|------|
        | � **Live Application** | [$DEPLOY_URL]($DEPLOY_URL) |
        | 🏥 **Health Endpoint** | [$DEPLOY_URL/api/health]($DEPLOY_URL/api/health) |
        | 📊 **Render Dashboard** | [View Build Logs](https://dashboard.render.com) |
        
        EOF
        
          if [ "$HEALTH_STATUS" = "success" ]; then
            cat << EOF >> $GITHUB_STEP_SUMMARY
        ---
        
        <div align="center">
        
        ## ✅ 🎉 Application is Healthy! 🎉 ✅
        
        Your application is **live** and **responding** to requests!
        
        ### Quick Actions
        
        [🌐 Open App]($DEPLOY_URL) • [🏥 Check Health]($DEPLOY_URL/api/health) • [📊 View Logs](https://dashboard.render.com)
        
        </div>
        
        EOF
          else
            cat << EOF >> $GITHUB_STEP_SUMMARY
        ### ⚠️ Health Check Notice
        
        The deployment was successful, but the health check timed out. This doesn't necessarily mean the deployment failed.
        
        **Action Items:**
        1. ✅ Check [Render Dashboard](https://dashboard.render.com) for build status
        2. ✅ Manually verify: [$DEPLOY_URL]($DEPLOY_URL)
        3. ✅ Review Render logs for any startup errors
        
        EOF
          fi
        else
          cat << EOF >> $GITHUB_STEP_SUMMARY
        ### ❌ Deployment Failed
        
        **Troubleshooting Steps:**
        1. Review the failed job logs above
        2. Check [Render Dashboard](https://dashboard.render.com) for errors
        3. Verify all secrets are configured correctly
        4. Check `render.yaml` configuration
        
        EOF
        fi
        
        cat << EOF >> $GITHUB_STEP_SUMMARY
        ---
        
        ## � Workflow Architecture
        
        \`\`\`mermaid
        graph LR
            A[🎨 Quality] --> D[🏗️ Build]
            B[🧪 Tests] --> D
            C[🔒 Security] --> D
            D --> E[🚀 Deploy]
            E --> F[🏥 Health Check]
            F --> G[📊 Dashboard]
            
            style A fill:#e1f5ff
            style B fill:#e1f5ff
            style C fill:#e1f5ff
            style D fill:#fff3e0
            style E fill:#f3e5f5
            style F fill:#e8f5e9
            style G fill:#fce4ec
        \`\`\`
        
        **Pipeline Features:**
        - ⚡ Parallel execution for pre-deploy checks (3x faster)
        - 🎯 Smart health check with exponential backoff
        - 📊 Comprehensive metrics tracking
        - 🔄 Automatic retry logic
        - 🚨 Emergency deployment mode available
        
        ---
        
        ## 🎯 Next Steps
        
        EOF
        
        if [ "$DEPLOY_STATUS" = "success" ] && [ "$HEALTH_STATUS" = "success" ]; then
          cat << EOF >> $GITHUB_STEP_SUMMARY
        ✅ **Deployment completed successfully!** Your changes are now live.
        
        **Recommended Actions:**
        - 🧪 Perform smoke tests on the live application
        - 📊 Monitor application metrics and logs
        - 👀 Review user feedback and error reports
        - 🔄 Consider monitoring the first few minutes for any issues
        
        EOF
        else
          cat << EOF >> $GITHUB_STEP_SUMMARY
        ⚠️ **Review Required:** Please check the logs and take appropriate action.
        
        **Need Help?**
        - 📖 Check [DEPLOYMENT.md](./DEPLOYMENT.md) for troubleshooting
        - 💬 Contact the team for deployment support
        - 🔧 Use emergency deployment mode if needed
        
        EOF
        fi
        
        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "*Generated by StarMock CI/CD Pipeline • [View Workflow](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})*" >> $GITHUB_STEP_SUMMARY
